# alex.anodec.ai

Digital business card of Alexander Stitsey (ANODEC AI). Static site, deployed on Vercel.
Source of truth for texts: `card.json` in the ANODEC StartupBase deliverables folder; rebuild with `build.py`.
